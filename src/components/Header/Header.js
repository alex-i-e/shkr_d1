import React, {Component} from 'react';
import './Header.css';

const Header = () => (
    <div>
        <header className="App-header">
            <h1 className="App-title">Welcome to Terminal</h1>
        </header>
        <p className="App-intro">
            To get started, choose the appropriated operator:
        </p>
    </div>
);

export default Header;
